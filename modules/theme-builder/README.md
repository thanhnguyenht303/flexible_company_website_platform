# Theme Builder Module

Owns theme settings types and admin theme behavior.

Theme settings include colors, typography, border radius, header/footer layout values, custom CSS, and an optional background image stored under `Images/theme-backgrounds/site/`.
