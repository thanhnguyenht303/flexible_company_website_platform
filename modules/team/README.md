# Team Module

Owns employee/team profile validation, ordering, visibility, localized role/bio fields, photo uploads, and public team rendering.

Team photos are stored under `Images/team/<team-member-id>/` and linked through `photoId`.
