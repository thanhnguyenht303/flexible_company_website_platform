# Page Builder Module

Owns visual builder block types, defaults, validation, draft conversion, and builder section utilities.

Drafts are stored in `PageBuilderDraft`. Publishing replaces live builder `PageSection` rows for the target page. Builder image uploads are stored under `Images/page-builder/<page-slug>/`.
