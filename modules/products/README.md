# Products Module

Owns product validation and public/admin product behavior.

Products support draft, published, and archived states; English/Vietnamese name, summary, and description fields; slug generation; and image galleries. Public routes show only `PUBLISHED` products. Gallery images are stored under `Images/products/<product-id>/`.
