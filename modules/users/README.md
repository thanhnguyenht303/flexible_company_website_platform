# Users Module

Owns roles, permissions, admin users, Super Admin bootstrap rules, password policy, and future invitation flows.

The first Super Admin is created with `pnpm setup:admin` while `ALLOW_ADMIN_BOOTSTRAP=true`. Super Admin permissions are stored as `{ all: true }`.
